

1. Each column in results matches a column from EBPP pan cancer expression data.

2. How many columns are there compared to xCell?  (11069 in ebpp), (9947 in xcell)
  -- but sample files go to 11070?  because it starts at sample_2.rda

3. Writing the tables, each 200 columns wide, to gzip tsv files.
  -- may have overlaps?  have all samples?

4. Wrote a file with the names of each parRes file in order
  --  the first row in each parRes file is the sample number
  --  which is the same column number in the EB++ data
  --  in the list of files, second row is a range of indices

5. 

